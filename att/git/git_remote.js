/**
 * An object representing a remote described in the output of `git remote -v`.
 *
 * @typedef {GitRemote}
 * @property {string} name
 * @property {object} url
 * @property {string} url.fetch
 * @property {string} url.push
 */
